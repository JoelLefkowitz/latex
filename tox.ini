[tox]
envlist = 
    py36
    py37
    py38

[testenv]
deps = 
    .[tests]
    pytest
    pytest-cov
    
commands = 
    coverage run --include latex/* -m    \
    pytest --doctest-modules latex    

    coverage run --include latex/* -a -m \
    pytest

    coverage report
    coverage xml
